# =====================================================
# PicoClaw Skills — Environment Variables Reference
# =====================================================
# Save this as ~/.picoclaw/.env.picoclaw (or point PICOCLAW_ENV_FILE to another path).
#
# Your config.json already handles:
#   ✅ OPENAI_API_KEY (in model_list)
#   ✅ TELEGRAM_BOT_TOKEN (in channels.telegram)
#   ✅ TELEGRAM_USER_ID (in channels.telegram.allow_from)
#
# The NEW skills need these ADDITIONAL env vars.
# Add them to your Pi environment however you prefer:
#   Option A: Export in ~/.bashrc or ~/.profile
#   Option B: Add to your systemd service file (EnvironmentFile=)
#   Option C: Export before running `picoclaw gateway`
# =====================================================

# ── Workspace security mode ──────────────
# Set false so git-summary can scan repos outside ~/.picoclaw/workspace
export PICOCLAW_AGENTS_DEFAULTS_RESTRICT_TO_WORKSPACE=false

# ── Email Digest skill ───────────────────
# Needed by: skills/email-digest
# How to get: Gmail > Settings > "App Passwords" (requires 2FA)
export EMAIL_IMAP_HOST=imap.gmail.com
export EMAIL_IMAP_PORT=993
export EMAIL_ADDRESS=you@gmail.com
export EMAIL_PASSWORD=xxxx-xxxx-xxxx-xxxx

# ── Todoist Manager skill ────────────────
# Needed by: skills/todoist-manager, skills/morning-briefing
# How to get: todoist.com > Settings > Integrations > Developer > API token
export TODOIST_API_TOKEN=xxxx

# ── Slack Digest skill ───────────────────
# Needed by: skills/slack-digest
# How to get:
#   1. api.slack.com/apps > Create New App > From Scratch
#   2. OAuth & Permissions > Bot Token Scopes: channels:history, channels:read
#   3. Install to Workspace > Copy "Bot User OAuth Token"
#   4. Channel ID: right-click channel > "View channel details" > copy ID at bottom
export SLACK_BOT_TOKEN=xoxb-xxx
export SLACK_CHANNEL_ID=C0XXXXXXX

# ── Google Calendar (optional) ───────────
# Needed by: skills/morning-briefing (calendar part)
# How to get: console.cloud.google.com > IAM > Service Accounts > Create > Download JSON
# If you skip this, morning briefing will still show weather + Todoist (no calendar)
export GOOGLE_CALENDAR_ID=primary
export GOOGLE_SERVICE_ACCOUNT_JSON=/home/your-user/.picoclaw/google-sa.json

# ── Git Summary skill ───────────────────
# Needed by: skills/git-summary
# Just list your local repo paths on the Pi, comma-separated
export GIT_REPOS=/home/your-user/projects/picoclaw,/home/your-user/projects/another-repo

# ── Weather ──────────────────────────────
# Morning briefing uses wttr.in (FREE, no API key needed)
# Just set your city name
export WEATHER_CITY=Chennai

# ── Timezone ─────────────────────────────
export TZ=Asia/Kolkata

# ── Optional: Figma ──────────────────────
# How to get: figma.com > Settings > Personal Access Tokens
# export FIGMA_ACCESS_TOKEN=xxxx
# export FIGMA_FILE_KEY=xxxx

# ── Optional: Linear ────────────────────
# How to get: linear.app > Settings > API > Personal API Keys
# export LINEAR_API_KEY=lin_api_xxxx
